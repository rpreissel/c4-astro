---
title: C4View Properties Test
description: Test page demonstrating all available C4View component properties
---

import C4View from '@components/C4ViewEcommerce.astro'

This page demonstrates all available properties of the `C4View` component.

## Default Configuration

The simplest usage with just `viewId`:

```astro
<C4View viewId="index" />
```

<C4View viewId="index" />

---

## Background Patterns

### Transparent (default)

```astro
<C4View viewId="index" background="transparent" />
```

<C4View viewId="index" background="transparent" />

### Dots

```astro
<C4View viewId="index" background="dots" />
```

<C4View viewId="index" background="dots" />

### Lines

```astro
<C4View viewId="index" background="lines" />
```

<C4View viewId="index" background="lines" />

### Cross

```astro
<C4View viewId="index" background="cross" />
```

<C4View viewId="index" background="cross" />

### Solid

```astro
<C4View viewId="index" background="solid" />
```

<C4View viewId="index" background="solid" />

---

## Controls

Show the control panel in the top-left corner:

```astro
<C4View viewId="index" controls={true} />
```

<C4View viewId="index" controls={true} />

---

## Pannable & Zoomable

Enable panning and zooming for interactive exploration:

```astro
<C4View viewId="index" pannable={true} zoomable={true} controls={true} />
```

<C4View viewId="index" pannable={true} zoomable={true} controls={true} />

---

## Color Schemes

### Light Mode

```astro
<C4View viewId="index" colorScheme="light" />
```

<C4View viewId="index" colorScheme="light" />

### Dark Mode

```astro
<C4View viewId="index" colorScheme="dark" />
```

<C4View viewId="index" colorScheme="dark" />

---

## Browser Popup

### Enabled (default)

Click on the diagram to open the browser popup:

```astro
<C4View viewId="index" browser={true} />
```

<C4View viewId="index" browser={true} />

### Disabled

No popup on click:

```astro
<C4View viewId="index" browser={false} />
```

<C4View viewId="index" browser={false} />

### Custom Browser Configuration

```astro
<C4View 
  viewId="index" 
  browser={{
    background: 'dots',
    controls: true,
    showNavigationButtons: true,
    enableSearch: true,
    enableFocusMode: true,
    enableElementDetails: true,
    enableRelationshipDetails: true,
    enableNotations: true,
  }}
/>
```

<C4View 
  viewId="index" 
  browser={{
    background: 'dots',
    controls: true,
    showNavigationButtons: true,
    enableSearch: true,
    enableFocusMode: true,
    enableElementDetails: true,
    enableRelationshipDetails: true,
    enableNotations: true,
  }}
/>

---

## Focus Mode

Double-click on a node to highlight incoming/outgoing edges:

```astro
<C4View viewId="index" enableFocusMode={true} browser={false} />
```

<C4View viewId="index" enableFocusMode={true} browser={false} />

---

## Element Details

Enable modal with element details on click:

```astro
<C4View viewId="index" enableElementDetails={true} browser={false} />
```

<C4View viewId="index" enableElementDetails={true} browser={false} />

---

## Element Tags

Display element tags in the bottom-left corner:

```astro
<C4View viewId="index" enableElementTags={true} />
```

<C4View viewId="index" enableElementTags={true} />

---

## Relationship Details

Show dropdown with details on relationship label click:

```astro
<C4View viewId="index" enableRelationshipDetails={true} browser={false} />
```

<C4View viewId="index" enableRelationshipDetails={true} browser={false} />

---

## Navigation Buttons

Show back/forward navigation buttons (useful with controls):

```astro
<C4View viewId="index" controls={true} showNavigationButtons={true} />
```

<C4View viewId="index" controls={true} showNavigationButtons={true} />

---

## Notations

Display view notations (if available):

```astro
<C4View viewId="index" enableNotations={true} />
```

<C4View viewId="index" enableNotations={true} />

---

## Fit View Padding

### Number (pixels)

```astro
<C4View viewId="index" fitViewPadding={50} />
```

<C4View viewId="index" fitViewPadding={50} />

### With x/y padding

```astro
<C4View viewId="index" fitViewPadding={{ x: 100, y: 20 }} />
```

<C4View viewId="index" fitViewPadding={{ x: 100, y: 20 }} />

---

## Keep Aspect Ratio

### Enabled (default)

```astro
<C4View viewId="index" keepAspectRatio={true} />
```

<C4View viewId="index" keepAspectRatio={true} />

### Disabled

```astro
<C4View viewId="index" keepAspectRatio={false} />
```

<C4View viewId="index" keepAspectRatio={false} />

---

## Reduce Graphics

Performance optimization for large diagrams:

```astro
<C4View viewId="index" reduceGraphics={true} />
```

<C4View viewId="index" reduceGraphics={true} />

---

## Dynamic View Variants

### As Diagram (default)

```astro
<C4View viewId="checkoutSequence" dynamicViewVariant="diagram" />
```

<C4View viewId="checkoutSequence" dynamicViewVariant="diagram" />

### As Sequence Diagram

```astro
<C4View viewId="checkoutSequence" dynamicViewVariant="sequence" />
```

<C4View viewId="checkoutSequence" dynamicViewVariant="sequence" />

---

## Dynamic View Walkthrough

Enable step-by-step walkthrough for dynamic views:

```astro
<C4View viewId="checkoutSequence" enableDynamicViewWalkthrough={true} controls={true} />
```

<C4View viewId="checkoutSequence" enableDynamicViewWalkthrough={true} controls={true} />

---

## Full Featured Example

All features enabled:

```astro
<C4View 
  viewId="index" 
  pannable={true}
  zoomable={true}
  controls={true}
  background="dots"
  showNavigationButtons={true}
  enableNotations={true}
  enableFocusMode={true}
  enableElementDetails={true}
  enableElementTags={true}
  enableRelationshipDetails={true}
  enableRelationshipBrowser={true}
  browser={{
    background: 'dots',
    controls: true,
    showNavigationButtons: true,
    enableSearch: true,
    enableFocusMode: true,
    enableDynamicViewWalkthrough: true,
    enableElementDetails: true,
    enableRelationshipBrowser: true,
    enableRelationshipDetails: true,
    enableElementTags: true,
    enableNotations: true,
    enableCompareWithLatest: true,
  }}
/>
```

<C4View 
  viewId="index" 
  pannable={true}
  zoomable={true}
  controls={true}
  background="dots"
  showNavigationButtons={true}
  enableNotations={true}
  enableFocusMode={true}
  enableElementDetails={true}
  enableElementTags={true}
  enableRelationshipDetails={true}
  enableRelationshipBrowser={true}
  browser={{
    background: 'dots',
    controls: true,
    showNavigationButtons: true,
    enableSearch: true,
    enableFocusMode: true,
    enableDynamicViewWalkthrough: true,
    enableElementDetails: true,
    enableRelationshipBrowser: true,
    enableRelationshipDetails: true,
    enableElementTags: true,
    enableNotations: true,
    enableCompareWithLatest: true,
  }}
/>
